pluginManagement {
	repositories {
		maven { url 'https://maven.fabricmc.net/' }
		gradlePluginPortal()
	}
}

dependencyResolutionManagement {
	versionCatalogs {
		create("libs") {
			from(files("libs.versions.toml"))
		}
	}
}

// 仅供 GitHub Hosted Runner 的端到端兼容性验证使用；不参与主模组发布。
include("ci-probe")
